import { type NextPage } from "next";
import BaseConverterComponent from "~/pageComponents/conversorbases/ConversorBasesPage";

const BaseConverterPage: NextPage = () => {
  return <BaseConverterComponent />;
};
export default BaseConverterPage;
